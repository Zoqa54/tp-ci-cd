// Pipeline declaratif Jenkins (TP1, variante) - SQUELETTE A COMPLETER.
//
// Objectif : ecrire un pipeline declaratif comportant les etapes
//   Checkout, Build, Test et Deploy.
//
// Completez les sections marquees TODO.

pipeline {
    // TODO 1 : indiquer sur quel agent executer le pipeline (agent any).
    agent any

    // Astuce : vous pouvez definir une variable d'environnement pour le
    // dossier de l'application.
    // environment {
    //     APP_DIR = 'jour-2-automatisation-conteneurs/code/starter/app'
    // }

    stages {
        // TODO 2 : etape Checkout - recuperer le code source (checkout scm).

        // TODO 3 : etape Build - se placer dans app/ et lancer "npm ci".

        // TODO 4 : etape Test - se placer dans app/ et lancer "npm test".

        // TODO 5 : etape Deploy - executee uniquement sur la branche main.
        //          Pour ce TP, un simple echo suffit.
    }

    // TODO 6 (optionnel) : ajouter un bloc "post" pour afficher un message
    //                      en cas de succes ou d'echec.
}
